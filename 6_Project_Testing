# 6. Project Testing Phase
**Project Name:** ComicVerse AI

## Test Cases & Verification Results

### 1. Endpoint Testing

* **`GET /` (Homepage):**
  * **Test:** Access input form on browser.
  * **Expected Result:** Renders `index.html` with form fields (prompt, character name, setting, tone, art style)[span_0](start_span)[span_0](end_span).
  * **Status:** Passed.

* **`POST /generate-comic/json` (Comic Generation Pipeline):**
  * **Test:** Submit form with sample prompt ("A brave fox exploring an enchanted forest")[span_1](start_span)[span_1](end_span).
  * **Expected Result:** Triggers Gemini models and Diffusers pipeline, saving panel images into `static/panels/` and returning structured JSON data[span_2](start_span)[span_2](end_span).
  * **Status:** Passed.

* **`GET /test-image` (Image Synthesis Verification):**
  * **Test:** Call test route to verify Hugging Face Diffusers pipeline[span_3](start_span)[span_3](end_span).
  * **Expected Result:** Generates a test image using Stable Diffusion and saves it locally without errors[span_4](start_span)[span_4](end_span).
  * **Status:** Passed.

* **`GET /export-pdf` (PDF Compilation Test):**
  * **Test:** Trigger PDF export function after previewing generated panels[span_5](start_span)[span_5](end_span).
  * **Expected Result:** `exporters.py` executes FPDF compilation, saves timestamped `.pdf` file under `static/exports/`, and redirects to `export_success.html`[span_6](start_span)[span_6](end_span).
  * **Status:** Passed.

## Validation Scenarios

1. **Custom Style Switching:** Tested changing tone to "funny" and art style to "comic book" (Scenario 2)[span_7](start_span)[span_7](end_span). The pipeline correctly adjusted generated narrative text and image style prompts[span_8](start_span)[span_8](end_span).
2. **PDF File Structure:** Verified that the exported PDF includes all panel images along with their corresponding narration text on consecutive pages[span_9](start_span)[span_9](end_span).

